- name: install latest qemu-guest-agent
  hosts: all
  ignore_unreachable: yes
  tasks:
    - name: qemu-guest-agent
      apt:
        name: qemu-guest-agent
        state: present
        update_cache: true
      become: true
